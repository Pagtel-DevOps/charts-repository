apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "activation-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "activation-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "activation-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

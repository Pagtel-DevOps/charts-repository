apiVersion: v1
kind: Service
metadata:
  name: {{ include "billet-microservice.fullname" .  }}
  labels:
    {{- include "billet-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "billet-microservice.selectorLabels" . | nindent 4 }}

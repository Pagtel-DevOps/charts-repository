apiVersion: v1
kind: Service
metadata:
  name: {{ include "communication-gateway.fullname" .  }}
  labels:
    {{- include "communication-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "communication-gateway.selectorLabels" . | nindent 4 }}

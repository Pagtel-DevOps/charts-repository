apiVersion: v1
kind: Service
metadata:
  name: {{- include "authenticator-microservice.fullname" .  }}
  labels:
    {{- include "authenticator-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "authenticator-microservice.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "portability-gateway-server.fullname" .  }}
  labels:
    {{- include "portability-gateway-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "portability-gateway-server.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backoffice-api.fullname" . }}-test-connection"
  labels:
    {{- include "backoffice-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backoffice-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailer-microservice-server.fullname" . }}-test-connection"
  labels:
    {{- include "mailer-microservice-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailer-microservice-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

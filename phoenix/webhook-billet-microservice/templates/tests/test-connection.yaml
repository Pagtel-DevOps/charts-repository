apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webhook-billet-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "webhook-billet-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webhook-billet-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

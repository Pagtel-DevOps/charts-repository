apiVersion: v1
kind: Service
metadata:
  name: {{ include "card-microservice.fullname" .  }}
  labels:
    {{- include "card-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "card-microservice.selectorLabels" . | nindent 4 }}

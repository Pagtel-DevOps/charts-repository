apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payment-webhook-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "payment-webhook-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payment-webhook-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

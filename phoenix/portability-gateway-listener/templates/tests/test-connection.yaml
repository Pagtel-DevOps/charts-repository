apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portability-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "portability-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portability-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "account-management-gateway.fullname" .  }}
  labels:
    {{- include "account-management-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "account-management-gateway.selectorLabels" . | nindent 4 }}

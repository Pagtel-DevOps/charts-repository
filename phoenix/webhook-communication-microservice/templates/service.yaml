apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-communication-microservice.fullname" .  }}
  labels:
    {{- include "webhook-communication-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "webhook-communication-microservice.selectorLabels" . | nindent 4 }}

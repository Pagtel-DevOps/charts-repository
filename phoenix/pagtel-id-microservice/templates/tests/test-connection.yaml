apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pagtel-id-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "pagtel-id-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pagtel-id-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "pagtel-id-microservice.fullname" .  }}
  labels:
    {{- include "pagtel-id-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "pagtel-id-microservice.selectorLabels" . | nindent 4 }}

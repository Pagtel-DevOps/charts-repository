apiVersion: v1
kind: Service
metadata:
  name: {{ include "communication-gateway-server.fullname" .  }}
  labels:
    {{- include "communication-gateway-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "communication-gateway-server.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "activation-gateway-server.fullname" . }}-test-connection"
  labels:
    {{- include "activation-gateway-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "activation-gateway-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "recharge-and-plan-gateway-server.fullname" . }}-test-connection"
  labels:
    {{- include "recharge-and-plan-gateway-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "recharge-and-plan-gateway-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

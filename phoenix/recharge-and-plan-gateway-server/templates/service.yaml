apiVersion: v1
kind: Service
metadata:
  name: {{- include "recharge-and-plan-gateway-server.fullname" .  }}
  labels:
    {{- include "recharge-and-plan-gateway-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "recharge-and-plan-gateway-server.selectorLabels" . | nindent 4 }}

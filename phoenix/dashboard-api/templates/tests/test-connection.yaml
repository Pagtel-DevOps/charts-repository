apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dashboard-api.fullname" . }}-test-connection"
  labels:
    {{- include "dashboard-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dashboard-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

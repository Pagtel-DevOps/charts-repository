apiVersion: v1
kind: Service
metadata:
  name: {{- include "financial-transaction-gateway.fullname" .  }}
  labels:
    {{- include "financial-transaction-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      
  selector:
    {{- include "financial-transaction-gateway.selectorLabels" . | nindent 4 }}

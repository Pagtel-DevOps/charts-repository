apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "financial-transaction-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "financial-transaction-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "financial-transaction-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "account-management-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "account-management-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "account-management-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

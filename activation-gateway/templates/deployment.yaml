apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "autenticador.fullname" . }}
  labels:
    {{- include "autenticador.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "autenticador.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "autenticador.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "autenticador.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: NODE_ENV

            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: PORT

            - name: BASE_URI
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: BASE_URI
            
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: DB_PORT

            - name: DB_DIALECT
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: DB_DIALECT

            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: DB_NAME

            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: DB_HOST

            - name: API_MAIL
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: API_MAIL

            - name: RABBIT_HOST
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: RABBIT_HOST

            - name: RABBIT_PORT
              valueFrom:
                configMapKeyRef:
                  name: gateway-ativacao
                  key: RABBIT_PORT

            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: ENCRYPTION_KEY

            - name: ENCRYPTION_IV
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: ENCRYPTION_IV

            - name: PAYLOAD_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: PAYLOAD_ENCRYPTION_KEY

            - name: PAYLOAD_ENCRYPTION_IV
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: PAYLOAD_ENCRYPTION_IV

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: DB_PASSWORD

            - name: RABBIT_USER
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: RABBIT_USER

            - name: RABBIT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gateway-ativacao
                  key: RABBIT_PASSWORD
                  
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mssql-db.fullname" . }}-test-connection"
  labels:
    {{- include "mssql-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mssql-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

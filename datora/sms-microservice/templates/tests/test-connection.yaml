apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datora-sms-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "datora-sms-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datora-sms-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

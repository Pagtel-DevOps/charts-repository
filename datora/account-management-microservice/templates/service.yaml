apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name  }}
  labels:
    {{- include "datora-account-management-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "datora-account-management-microservice.selectorLabels" . | nindent 4 }}

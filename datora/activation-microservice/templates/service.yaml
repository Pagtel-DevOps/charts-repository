apiVersion: v1
kind: Service
metadata:
  name: {{- include "datora-activation-microservice.fullname" .  }}
  labels:
    {{- include "datora-activation-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "datora-activation-microservice.selectorLabels" . | nindent 4 }}

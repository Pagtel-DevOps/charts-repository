apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datora-activation-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "datora-activation-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datora-activation-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

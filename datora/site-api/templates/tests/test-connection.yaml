apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datora-site-api.fullname" . }}-test-connection"
  labels:
    {{- include "datora-site-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datora-site-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

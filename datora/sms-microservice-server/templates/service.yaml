apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-microservice-server.fullname" .  }}
  labels:
    {{- include "sms-microservice-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      
  selector:
    {{- include "sms-microservice-server.selectorLabels" . | nindent 4 }}

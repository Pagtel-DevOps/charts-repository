apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sms-microservice-server.fullname" . }}-test-connection"
  labels:
    {{- include "sms-microservice-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sms-microservice-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
